using UnityEngine;
using System.Collections;

public class RedCubeTrigger : MonoBehaviour 
{
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

    //void OnTriggerEnter(Collider other)
    //{
    //    if (other.gameObject.name == GameObject.FindObjectOfType<Player1MoveScript>().name && !GameObject.FindObjectOfType<PowerUpScriptP1>().Invulnerable && !GameObject.FindObjectOfType<Jumpscript>().JumpP1)
    //    {
    //        GameObject.FindObjectOfType<Player1LevelScript>().Speed = 2;
    //    }
    //    else if (other.gameObject.name == GameObject.FindObjectOfType<Player2MoveScript>().name && !GameObject.FindObjectOfType<PowerUpScriptP2>().Invulnerable && !GameObject.FindObjectOfType<Jumpscript>().JumpP2)
    //    {
    //        GameObject.FindObjectOfType<Player1LevelScript>().Speed = 2;
    //    }
    //}
}
